export interface Heading3Props {
    label: string;
    bold?: boolean;
    classname?: string;
  }
  
  export interface LabelProps {
    label: string;
    classname?: string;
  }
  
  export interface ParagraphProps extends Heading3Props {
    label: string;
    classname?: string;
  }
  